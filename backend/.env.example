# Browser origins allowed to call this API (comma-separated, no trailing slash).
ALLOWED_ORIGINS=http://localhost:3000
APP_ENV=development

# --- LATER (refresh job only; NEVER needed by the deployed app) ---
# A keyed market-data provider (e.g. Finnhub), used only when building snapshots:
# FINNHUB_API_KEY=
# The Anthropic key for the build-time AI content pipeline (a later phase):
# ANTHROPIC_API_KEY=
